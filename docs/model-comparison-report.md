# LSTM vs GRU vs TCN 모델 통합 비교 보고서

> 작성일: 2026-04-15
> 작성자: B2 (수지니)
> 참조: `docs/lstm-model-report.md`, `docs/gru-model-report.md`, `docs/tcn-model-report.md`

---

## 목차

1. [비교 개요](#1-비교-개요)
2. [아키텍처 비교](#2-아키텍처-비교)
3. [학습 결과 비교](#3-학습-결과-비교)
4. [백테스트 성능 비교](#4-백테스트-성능-비교)
5. [종합 분석](#5-종합-분석)
6. [결론 및 향후 계획](#6-결론-및-향후-계획)

---

## 1. 비교 개요

### 1.1 비교 목적

마포구 프랜차이즈 상권분석 시뮬레이터의 매출 예측 기능에 사용할 최적 모델을 선정하기 위해
LSTM, GRU, TCN 세 가지 시계열 예측 모델을 동일 조건에서 구현하고 비교한다.

세 모델은 모두 마포구 분기별 매출 데이터(dong_code × industry_code 조합)를 기반으로
2024년 연간 매출을 예측하며, 실제 2024년 매출과의 오차를 측정하여 성능을 평가한다.

### 1.2 동일 조건 요약

세 모델이 공정하게 비교될 수 있도록 아래 조건을 완전히 통일하였다.

| 조건 | 공통값 | 비고 |
|------|--------|------|
| 피처 수 | 31개 (ALL_FEATURES) | SALES 12 + STORE 5 + POP 4 + RENT 2 + EXTRA 3 + GOLMOK 5 |
| 전처리 모듈 | `lstm_forecast/data_prep.py` 공용 | GRU·TCN은 직접 재사용 |
| Hot Deck 보간 | 동일 적용 | guide-density 방식 |
| EXCLUDE_COMBOS | 염리동 중식, 성산1동 제과 제외 | 매출 규모 극소 → MAPE 폭발 조합 |
| COVID 가중치 | 2020~2021: sample_weight=0.5 | 코로나 이상치 완화 |
| window_size | 4 (분기 단위) | 1년치 과거 데이터로 예측 |
| 전이학습 전략 | 사전학습 → 파인튜닝 2단계 | 서울 전체 사전학습 후 마포구 파인튜닝 |
| 백테스트 기준 | 2024년, **154건** | 156건 중 EXCLUDE_COMBOS 2건 제외 |
| 데이터 누수 방지 | `quarter < 20241` cutoff | 2024 Q1 이전 데이터만 입력으로 사용 |
| 추론 방식 | 자기회귀 4스텝 (sliding window) | log1p 역변환, 음수 방지 |
| 신뢰구간 | 95% (z=1.96), 선형 불확실성 증가 | 5% × step 증가 |
| 실행 환경 | Docker container (backend), CPU only | AWS RDS PostgreSQL 연결 |

---

## 2. 아키텍처 비교

### 2.1 구조 개요

| 항목 | LSTM | GRU | TCN |
|------|------|-----|-----|
| **구조 유형** | 순환 신경망 (RNN) | 순환 신경망 (RNN) | 팽창 인과 컨볼루션 |
| **게이트 수** | 3개 (input, forget, output) | 2개 (reset, update) | 없음 |
| **hidden state** | h + c (cell state) | h만 | 없음 |
| **병렬 처리** | 불가 (시퀀셜) | 불가 (시퀀셜) | 가능 |
| **기울기 소실 완화** | cell state (forget gate) | update gate | Residual connection |
| **수용 영역 제어** | hidden state 간접 | hidden state 간접 | dilations=[1,2]로 직접 (RF=4) |
| **시퀀스 집약** | Attention (가중 합산) | Attention (가중 합산) | 마지막 타임스텝 `[:,:,-1]` |
| **파라미터 수** | 약 229K (최다) | 약 168K | 약 145K (최소) |
| **epoch당 학습 시간** | ~8초 | ~7.8초 | ~25초 |

### 2.2 hidden state 구조 차이

```python
# LSTM: 두 가지 내부 상태 반환 (hidden state + cell state)
lstm_out, (h, c) = self.lstm(x)   # h: 단기, c: 장기 기억 분리

# GRU: hidden state만 반환 (cell state 없음)
gru_out, _ = self.gru(x)          # h만으로 단/장기 기억 통합 처리

# TCN: 순환 구조 없음 — 입력을 한 번에 병렬 처리
x = self.input_proj(x)            # Linear 투영 (31 → 128)
x = x.transpose(1, 2)             # (batch, seq, ch) → (batch, ch, seq)
for block in self.tcn_blocks:
    x = block(x)                  # TemporalBlock 순차 통과
out = x[:, :, -1]                 # 마지막 타임스텝만 사용
```

LSTM의 cell state는 forget gate를 통해 장기 패턴(예: 코로나 전후 구조 변화)을 명시적으로 보존한다.
GRU는 cell state 없이도 update gate로 유사한 역할을 수행하며 파라미터가 적다.
TCN은 hidden state 자체가 없고, 팽창 컨볼루션의 수용 영역(RF=4)으로 시퀀스 전체를 한 번에 처리한다.

### 2.3 hidden_size vs n_channels 차이

| 모델 | 파라미터명 | 값 | 의미 |
|------|-----------|-----|------|
| LSTM | `hidden_size` | 128 | RNN 내부 상태 벡터 차원 수 |
| GRU | `hidden_size` | 128 | RNN 내부 상태 벡터 차원 수 |
| TCN | `n_channels` | 128 | 각 TemporalBlock의 Conv1d 채널 수 |

숫자는 128로 동일하지만 개념이 다르다.
`hidden_size`는 순환 연산에서 매 타임스텝 전달되는 상태 벡터의 크기이고,
`n_channels`는 컨볼루션 필터의 개수(출력 채널 수)이다.
두 값을 동일하게 설정하여 모델 용량(capacity)을 최대한 통일하고 공정한 비교를 도모했다.

### 2.4 아키텍처 흐름 비교

**LSTM / GRU (공통 구조):**
```
입력 (batch, 4, 31)
  → nn.LSTM / nn.GRU (hidden=128, layers=2)
  → Attention: Linear(128→64) → Tanh → Linear(64→1) → Softmax
  → context (batch, 128)
  → FC: Linear(128→64) → ReLU → Dropout → Linear(64→1)
  → 출력 (batch, 1)
```

**TCN (다른 구조):**
```
입력 (batch, 4, 31)
  → input_proj: Linear(31→128)
  → transpose → (batch, 128, 4)
  → TemporalBlock(dilation=1): CausalConv1d × 2 + LayerNorm + Residual
  → TemporalBlock(dilation=2): CausalConv1d × 2 + LayerNorm + Residual
  → 마지막 타임스텝 [:, :, -1] → (batch, 128)
  → FC: Linear(128→64) → ReLU → Dropout → Linear(64→1)
  → 출력 (batch, 1)
```

---

## 3. 학습 결과 비교

### 3.1 사전학습 (서울 전체 25개구, 87,938행)

| 항목 | LSTM | GRU | TCN |
|------|------|-----|-----|
| 설정 epoch | 100 | 100 | 100 |
| 실제 종료 epoch | **42** (조기종료) | 65 (조기종료) | 58 (조기종료) |
| **best val_loss** | 0.000336 | 0.000318 | **0.000287** |
| epoch당 시간 | ~8초 | ~7.8초 | ~25초 |
| 가중치 경로 | `pretrained.pt` | `pretrained_gru.pt` | `pretrained_tcn.pt` |

- TCN이 사전학습 val_loss 최저 — 전체 서울 데이터 피팅 성능은 TCN 우수
- TCN은 epoch당 약 3배 느림 (input_proj + 2개 TemporalBlock + LayerNorm transpose 오버헤드)
- LSTM이 가장 일찍 종료 (epoch 42) — Attention 레이어 추가로 인한 약한 과적합 경향

### 3.2 파인튜닝 1단계 (마포구, 3,703행 — RNN/TCN freeze, FC만 학습)

| 항목 | LSTM | GRU | TCN |
|------|------|-----|-----|
| 설정 epoch | 10 | 10 | 10 |
| 실제 종료 epoch | 10 (전체 완료) | 10 (전체 완료) | 10 (전체 완료) |
| **최종 val_loss** | 0.000503 | 0.000484 | 0.000441 |
| 학습률 | 5e-4 | 5e-4 | 5e-4 |

- TCN이 1단계에서도 val_loss 최저 — FC Head가 마포구 스케일에 빠르게 적응
- LSTM이 val_loss 가장 높음 — 파라미터 수가 많아 FC만으로는 충분히 적응하지 못함

### 3.3 파인튜닝 2단계 (전체 unfreeze, 낮은 학습률)

| 항목 | LSTM | GRU | TCN |
|------|------|-----|-----|
| 설정 epoch | 50 | 50 | 50 |
| 실제 종료 epoch | 27 (조기종료) | **13** (조기종료) | **12** (조기종료) |
| **best val_loss** | 0.000479 | 0.000462 | 0.000420 |
| 학습률 | 1e-4 | 1e-4 | 1e-4 |
| 가중치 경로 | `finetuned_mapo.pt` | `finetuned_mapo_gru.pt` | `finetuned_mapo_tcn.pt` |

- TCN이 파인튜닝 val_loss도 최저 (0.000420) — 손실 함수 기준 최우수
- GRU·TCN은 13·12 epoch에서 조기종료 — 소규모 마포구 데이터에서 빠르게 수렴
- LSTM은 27 epoch까지 지속 개선 — 파라미터가 많아 세밀한 조정 기간 필요

---

## 4. 백테스트 성능 비교

```
평가 기준: 2024년 마포구 동×업종 조합
샘플 수:   154건 (156건 중 EXCLUDE_COMBOS 2건 제외)
제외 조합: 염리동 중식 (MAPE 600%+), 성산1동 제과 (MAPE 300%+)
데이터 누수 방지: 2024 Q1 이전 데이터만 입력으로 사용
```

### 4.1 전체 정확도

| 지표 | LSTM | GRU | TCN | 최우수 |
|------|------|-----|-----|--------|
| **MAPE** | **10.5%** | 11.4% | 12.6% | **LSTM** |
| **MAE** | **10.3억** | 14.8억 | 10.5억 | **LSTM** |
| **RMSE** | 38.5억 | 58.7억 | **25.6억** | **TCN** |
| **R²** | 0.9829 | 0.9603 | **0.9925** | **TCN** |

- **LSTM**: MAPE·MAE 최우수 — 전 조합에서 균형 잡힌 상대 오차
- **TCN**: RMSE·R² 최우수 — 대형 상권 절대 오차 최소, 분산 설명력 최고
- **GRU**: 모든 지표에서 중간 또는 최하 — 세 모델 중 전반적 성능 가장 낮음

### 4.2 동별 MAPE 전체 비교 (오름차순 — LSTM 기준 정렬)

| 동 | LSTM | GRU | TCN | LSTM 최우수 여부 |
|----|------|-----|-----|----------------|
| 상암동 | 4.8% | 3.4% | 6.8% | — (GRU 최우수) |
| 서강동 | 6.2% | **5.1%** | 11.1% | — (GRU 최우수) |
| 성산2동 | **6.4%** | 10.6% | 9.8% | ✅ |
| 대흥동 | 6.5% | **6.2%** | 8.9% | — (GRU 최우수) |
| 망원2동 | **7.2%** | 10.4% | 10.9% | ✅ |
| 합정동 | **8.3%** | 8.6% | 11.0% | ✅ |
| 공덕동 | **8.4%** | 10.8% | 11.9% | ✅ |
| 서교동 | 8.5% | 11.1% | **7.8%** | — (TCN 최우수) |
| 망원1동 | **11.0%** | 9.2% | 17.9% | — (GRU 최우수) |
| 도화동 | **11.1%** | 13.3% | 11.6% | ✅ |
| 연남동 | **11.7%** | 12.2% | 12.7% | ✅ |
| 용강동 | **14.6%** | 17.1% | 18.0% | ✅ |
| 아현동 | **14.7%** | 15.2% | 13.3% | — (TCN 최우수) |
| 염리동 | **15.1%** | 17.8% | 14.3% | — (TCN 최우수) |
| 성산1동 | **17.4%** | 16.9% | 18.0% | — (GRU 최우수) |
| 신수동 | **17.8%** | 15.7% | 18.8% | — (GRU 최우수) |

- LSTM 최우수: 9개 동 / GRU 최우수: 6개 동 / TCN 최우수: 4개 동
- LSTM이 가장 많은 동에서 최우수

### 4.3 업종별 MAPE 전체 비교 (오름차순 — LSTM 기준 정렬)

| 업종 | LSTM | GRU | TCN | 최우수 |
|------|------|-----|-----|--------|
| 일식음식점 | **5.7%** | 7.6% | 8.6% | **LSTM** |
| 한식음식점 | **6.4%** | 11.1% | 9.9% | **LSTM** |
| 커피-음료 | **7.0%** | 9.2% | 8.1% | **LSTM** |
| 중식음식점 | **8.7%** | 9.0% | 8.4% | — (TCN 최우수) |
| 분식전문점 | **8.7%** | 7.6% | 11.1% | — (GRU 최우수) |
| 치킨전문점 | **8.8%** | 9.7% | 12.7% | **LSTM** |
| 호프-간이주점 | **11.5%** | 10.8% | 11.5% | — (GRU 최우수) |
| 제과점 | 13.5% | **12.6%** | 15.8% | — (GRU 최우수) |
| 패스트푸드점 | **17.5%** | 18.2% | 19.1% | **LSTM** |
| 양식음식점 | **17.6%** | 17.9% | 21.5% | **LSTM** |

- LSTM 최우수: 6개 업종 / GRU 최우수: 3개 업종 / TCN 최우수: 1개 업종

---

## 4-1. 실험 A: 시드 설정 X — 3회 반복 실험

> 시드 미설정(torch/numpy/random seed 없음) 조건에서 3회 독립 학습 후 백테스트 결과.
> 각 회차는 사전학습 → 파인튜닝 → 백테스트 전 과정을 새로 실행.
> 평가 기준: 2024년 마포구 154개 동×업종 조합 (EXCLUDE_COMBOS 2건 제외)

### MAPE 비교 (3회)

| 모델 | run1 MAPE | run2 MAPE | run3 MAPE | 평균 | 표준편차 |
|------|-----------|-----------|-----------|------|----------|
| LSTM | 10.49% | 10.27% | 10.56% | **10.44%** | 0.15% |
| GRU  | 11.36% | 11.60% | 11.17% | **11.38%** | 0.22% |
| TCN  | 12.61% | 10.85% | 11.38% | **11.61%** | 0.90% |

### MAE 비교 (3회, 억원)

| 모델 | run1 MAE | run2 MAE | run3 MAE | 평균 |
|------|----------|----------|----------|------|
| LSTM | 10.3억 | 10.0억 | 10.6억 | **10.3억** |
| GRU  | 14.8억 | 12.6억 | 11.2억 | **12.9억** |
| TCN  | 10.5억 | 7.1억  | 10.9억 | **9.5억**  |

### RMSE 비교 (3회, 억원)

| 모델 | run1 RMSE | run2 RMSE | run3 RMSE | 평균 |
|------|-----------|-----------|-----------|------|
| LSTM | 38.5억 | 35.4억 | 38.0억 | **37.3억** |
| GRU  | 58.7억 | 37.2억 | 34.2억 | **43.4억** |
| TCN  | 25.6억 | 19.3억 | 29.0억 | **24.6억** |

### R² 비교 (3회)

| 모델 | run1 R² | run2 R² | run3 R² | 평균 |
|------|---------|---------|---------|------|
| LSTM | 0.9829 | 0.9856 | 0.9833 | **0.9839** |
| GRU  | 0.9603 | 0.9840 | 0.9865 | **0.9769** |
| TCN  | 0.9925 | 0.9957 | 0.9903 | **0.9928** |

### 종합 분석 (3회 평균 기준)

| 지표 | LSTM 평균 | GRU 평균 | TCN 평균 | 최우수 |
|------|-----------|----------|----------|--------|
| **MAPE** | **10.44%** | 11.38% | 11.61% | **LSTM** |
| **MAE** | 10.3억 | 12.9억 | **9.5억** | **TCN** |
| **RMSE** | 37.3억 | 43.4억 | **24.6억** | **TCN** |
| **R²** | 0.9839 | 0.9769 | **0.9928** | **TCN** |
| **MAPE 표준편차** | **0.15%** | 0.22% | 0.90% | **LSTM** |

- **LSTM**: MAPE 평균 최우수(10.44%), 표준편차 최소(0.15%) — 가장 일관된 상대 오차
- **TCN**: MAE·RMSE·R² 평균 최우수 — 대형 상권 절대 오차 최소, 분산 설명력 최고이나 회차 간 MAPE 변동 큼(0.90%)
- **GRU**: 3회 평균 모든 지표에서 LSTM·TCN보다 열위 — run1 RMSE(58.7억) 이상값이 평균을 크게 끌어올림

## 4-2. 실험 B: 시드 설정 O
→ 추후 추가 예정

---

## 5. 종합 분석

### 5.1 모델별 특성 요약

#### LSTM

| 항목 | 내용 |
|------|------|
| **예측 방식** | 3-gate 순환 신경망 + Attention 가중 합산 → 자기회귀 4스텝 |
| **장점** | cell state로 장기 패턴 보존 / Attention으로 중요 분기 강조 / 전 동·업종 균형 예측 |
| **단점** | 파라미터 최다(~229K) → 소규모 데이터에서 과적합 위험 / 학습 속도 느림 |
| **한계** | 마포구 156개 조합의 소규모 데이터에서는 파라미터 과잉 가능성 / 1회 학습 결과 |
| **비고** | MAPE·MAE 최우수 / 전반적으로 가장 균형 잡힌 성능 |

#### GRU

| 항목 | 내용 |
|------|------|
| **예측 방식** | 2-gate 순환 신경망 + Attention 가중 합산 → 자기회귀 4스텝 |
| **장점** | 파라미터 적음(~168K) → 소규모 데이터 과적합 위험 낮음 / LSTM과 유사 구조 |
| **단점** | cell state 없어 장기 의존성 표현력 LSTM보다 약함 / 모든 지표에서 세 모델 중 최하위 또는 중간 |
| **한계** | MAE 14.8억으로 LSTM(10.3억) 대비 크게 높음 — 대형 상권 절대 오차 큼 |
| **비고** | LSTM 대비 MAPE 차이 0.9%p (통계적으로 유의미한 차이인지 반복 실험 필요) |

#### TCN

| 항목 | 내용 |
|------|------|
| **예측 방식** | 팽창 인과 컨볼루션 (RF=4) → 마지막 타임스텝 → 자기회귀 4스텝 |
| **장점** | R²·RMSE 최우수 / 대형 상권 절대 오차 최소 / hidden state 누적 오차 없음 |
| **단점** | MAPE 12.6%로 세 모델 중 최고 / epoch당 학습 시간 3배 느림(~25초) |
| **한계** | RF=4 고정으로 4분기 이상 장기 패턴 반영 불가 / 소규모 불규칙 상권에서 상대 오차 큼 |
| **비고** | 파인튜닝 val_loss 최저(0.000420)지만 백테스트 MAPE는 최고 — 과소 표현 조합에서 취약 |

### 5.2 지표별 최우수 모델

| 지표 | 최우수 | 이유 |
|------|--------|------|
| MAPE (상대 오차) | **LSTM** | 전 조합 균등하게 상대 오차 낮음 — 소규모 상권에서도 안정 |
| MAE (절대 오차) | **LSTM** | 10.3억으로 GRU(14.8억) 대비 크게 낮음 |
| RMSE (이상값 민감도) | **TCN** | 대형 상권 예측 정밀도 우수 — 분산이 큰 매출에 강함 |
| R² (분산 설명력) | **TCN** | 0.9925로 거의 완벽한 분산 설명 |

### 5.3 마포구 데이터 특성 적합도 분석

마포구 데이터는 아래 특성을 갖는다:
- **소규모**: 154개 동×업종 조합, 마포구 분기 데이터 3,703행
- **이질적**: 상암동(대형 업무지구) ↔ 염리동(소규모 주거상권) — 매출 스케일 수백 배 차이
- **구조 변화**: 코로나(2020~2021) 전후 급격한 패턴 변화 존재
- **계절성**: 분기별 패턴이 규칙적인 동(상암동)과 불규칙한 동(신수동) 혼재

| 특성 | LSTM 적합도 | GRU 적합도 | TCN 적합도 |
|------|------------|-----------|-----------|
| 소규모 데이터 | 보통 (파라미터 과잉 가능) | 좋음 (파라미터 적음) | 좋음 (파라미터 최소) |
| 이질적 규모 | **좋음** (Attention이 중요 분기 강조) | 보통 | 보통 (절대 오차는 낮지만 상대 오차 큼) |
| 구조 변화 적응 | **좋음** (cell state 장기 패턴 보존) | 보통 | 보통 (RF=4 고정) |
| 균등 예측 | **좋음** (전 동·업종 균형) | 보통 | 나쁨 (소규모 상권 상대 오차 큼) |
| 학습 속도 | 보통 | **좋음** | 나쁨 (~3배 느림) |

**결론**: 마포구의 소규모·이질적 데이터 특성에서는 **LSTM이 가장 균형 잡힌 성능**을 보인다.
TCN은 대형 상권(상암동, 서교동)에서 강하지만 소규모 불규칙 상권에서 상대 오차가 커지는 경향이 있다.

---

## 6. 결론 및 향후 계획

### 6.1 선정 모델

**선정 모델: LSTM** (`models/lstm_forecast/`)

선정 근거:
- MAPE 10.5% — 세 모델 중 최우수
- MAE 10.3억 — 세 모델 중 최우수
- 16개 동 중 9개, 10개 업종 중 6개에서 최우수
- R²=0.9829로 매출 분산의 98.3% 설명 — 실용적으로 충분한 수준

### 6.2 주의 사항 — 1회 학습 기준임을 명시

LSTM vs GRU MAPE 차이는 **1.0%p (10.5% vs 11.4%)** 로 근소하다.

이 결과는 각 모델을 **1회만 학습한 결과**이며,
초기 가중치 랜덤 시드, 학습 데이터 배치 순서 등에 따라 결과가 달라질 수 있다.

| 관점 | 내용 |
|------|------|
| 1회 실험 | LSTM MAPE 10.5%, GRU MAPE 11.4% — LSTM 우세 |
| 반복 실험 시 | 시드에 따라 GRU가 동등하거나 LSTM을 역전할 가능성 존재 |
| 권장 | 3회 이상 반복 학습 후 평균 MAPE로 최종 비교하는 것이 통계적으로 더 엄밀함 |

현 단계에서는 LSTM을 선정하되, 향후 반복 실험으로 결과를 재확인하는 것을 권장한다.

### 6.3 simulation.py 연동 계획

선정 모델(LSTM)의 `predict()` 함수를 시뮬레이터 백엔드에 연동한다.

```python
# 연동 방법 — backend/src/simulation.py 또는 관련 라우터에서 호출
from models.lstm_forecast.predict import predict

result = predict(
    dong_code="11440555",    # 행정동 코드 (예: 상암동)
    industry_code="CS100001",  # 업종 코드 (예: 한식음식점)
    n_months=4,               # 4분기 예측 (기본값)
    config=None,              # DEFAULT_PREDICT_CONFIG 사용
)
# 반환: [{"quarter_offset": 1, "predicted_sales": ..., "confidence_lower": ..., "confidence_upper": ...}, ...]
```

필요 시 GRU 또는 TCN으로 전환도 동일한 인터페이스로 가능하다:
- `from models.gru_forecast.predict import predict`
- `from models.tcn_forecast.predict import predict`

### 6.4 향후 개선 방향

| 항목 | 내용 |
|------|------|
| 반복 실험 | 3회 이상 학습 후 평균 MAPE로 최종 모델 재확인 |
| 데이터 보완 | 2024 Q3~Q4 서울시 추정매출 공개 시 즉시 재학습 |
| EXCLUDE_COMBOS 고도화 | 현재 하드코딩 2개 → 자동 이상치 탐지 로직으로 대체 |
| 신뢰구간 개선 | 선형 증가(`5% × step`) → Monte Carlo Dropout 기반 불확실성 추정 |
| 앙상블 검토 | LSTM + TCN 예측 평균 — MAPE와 R² 동시 개선 가능성 |
| GPU 전환 | CPU → GPU 환경에서 TCN 재평가 (TCN은 병렬화 이점이 GPU에서 극대화) |
| 다변량 타겟 | monthly_sales 단일 → 점포당 매출, 폐업률 등 다변량 예측으로 확장 |

---

*이 문서는 LSTM·GRU·TCN 3개 모델의 동일 조건 비교 분석 결과를 기록한 것입니다.*
*수치는 각 모델 1회 학습 기준이며, 반복 실험 시 결과가 달라질 수 있습니다.*
*세부 학습 로그 및 구현 상세는 각 개별 보고서(`lstm/gru/tcn-model-report.md`)를 참조하세요.*
